@model IEnumerable<Application.ViewModels.CountryViewModel>

@{
    ViewData["Title"] = "Países";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0">Países</h2>
        <a asp-action="Create" class="btn bg-navy">
            <i class="bi bi-plus"></i> Crear Nuevo
        </a>
    </div>

    <div class="card">
        <div class="card-body">
            <table class="table table-hover">
                <thead class="table-light">
                    <tr>
                        <th>País</th>
                        <th>Código ISO</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>@item.IsoCode</td>
                            <td>
                                <a asp-action="Edit" asp-route-id="@item.Id" class="text-primary">Editar</a> |
                                <a asp-action="Details" asp-route-id="@item.Id" class="text-secondary">Detalles</a> |
                                <a asp-action="Delete" asp-route-id="@item.Id" class="text-danger">Eliminar</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

