@model Application.ViewModels.CountryViewModel

@{
    ViewData["Title"] = "Editar País";
}

<div class="container mt-4">
    <div class="card">
        <div class="card-body">
            <h2 class="mb-4">Editar País</h2>
            <form asp-action="Edit">
                <input type="hidden" asp-for="Id" />
                <div class="mb-3">
                    <label asp-for="Name" class="form-label">Nombre</label>
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <label asp-for="IsoCode" class="form-label">Código ISO</label>
                    <input asp-for="IsoCode" class="form-control" />
                    <span asp-validation-for="IsoCode" class="text-danger"></span>
                </div>
                <button type="submit" class="btn btn-primary bg-navy">Guardar</button>
                <a asp-action="Index" class="btn btn-secondary ms-2">Cancelar</a>
            </form>
        </div>
    </div>
</div>

